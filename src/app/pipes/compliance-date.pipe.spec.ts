import { ComplianceDatePipe } from './compliance-date.pipe';

describe('ComplianceDatePipe', () => {
  it('create an instance', () => {
    const pipe = new ComplianceDatePipe();
    expect(pipe).toBeTruthy();
  });
});
