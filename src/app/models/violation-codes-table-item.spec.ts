import { ViolationCodesTableItem } from './violation-codes-table-item';

describe('ViolationCodesTableItem', () => {
  it('should create an instance', () => {
    expect(new ViolationCodesTableItem()).toBeTruthy();
  });
});
